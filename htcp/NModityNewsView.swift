
import UIKit

class NModityNewsView: UIView {
    private var statubuttonGundong: [Any]!
private var attsOrientation: [Any]!
private var sublyoutMaskView: UIView!
private var purchasedEditPicker: String!

 var hourlabelRegister_4w: Float = 0.0
 var themeBundle: String!


    override init(frame: CGRect) {
        super.init(frame: frame)
        statubuttonGundong = [482, 964, 346]
        self.attsOrientation = [835, 632, 131]
        self.sublyoutMaskView = UIView(frame:CGRect.zero)
        purchasedEditPicker = String(cString: [104,105,103,104,98,105,116,115,0], encoding: .utf8)!

        self.init_materialMine()
    }

    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    private func init_materialMine() -> Void {
        
    }

    @discardableResult
 func questionChatPlace(nicknameCustom: [String: Any]!, utilsaAtts: [String: Any]!) -> UITableView! {
    var avatarsy: Double = 2.0
    _ = avatarsy
    var gesturew: String! = String(cString: [100,111,99,107,101,114,0], encoding: .utf8)!
    _ = gesturew
   if !gesturew.contains("\(avatarsy)") {
      avatarsy += Double(gesturew.count & 1)
   }
      gesturew.append("\(gesturew.count ^ 1)")
     let tipLook: UIImageView! = UIImageView()
     var styleSelectbutton: Bool = true
     let int_h5Minutes: UIImageView! = UIImageView()
     var epairLove: String! = String(cString: [119,104,101,110,0], encoding: .utf8)!
    var bleedFmtp = UITableView()
    bleedFmtp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    bleedFmtp.delegate = nil
    bleedFmtp.dataSource = nil
    bleedFmtp.alpha = 0.8;
    bleedFmtp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    bleedFmtp.frame = CGRect(x: 250, y: 100, width: 0, height: 0)
    tipLook.alpha = 0.6;
    tipLook.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    tipLook.frame = CGRect(x: 122, y: 319, width: 0, height: 0)
    tipLook.animationRepeatCount = 0
    tipLook.image = UIImage(named:String(cString: [115,104,111,119,0], encoding: .utf8)!)
    tipLook.contentMode = .scaleAspectFit
    
    var tipLookFrame = tipLook.frame
    tipLookFrame.size = CGSize(width: 262, height: 115)
    tipLook.frame = tipLookFrame
    if tipLook.alpha > 0.0 {
         tipLook.alpha = 0.0
    }
    if tipLook.isHidden {
         tipLook.isHidden = false
    }
    if !tipLook.isUserInteractionEnabled {
         tipLook.isUserInteractionEnabled = true
    }

    int_h5Minutes.alpha = 0.0;
    int_h5Minutes.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    int_h5Minutes.frame = CGRect(x: 85, y: 144, width: 0, height: 0)
    int_h5Minutes.contentMode = .scaleAspectFit
    int_h5Minutes.animationRepeatCount = 3
    int_h5Minutes.image = UIImage(named:String(cString: [111,116,104,101,114,0], encoding: .utf8)!)
    
    var int_h5MinutesFrame = int_h5Minutes.frame
    int_h5MinutesFrame.size = CGSize(width: 80, height: 253)
    int_h5Minutes.frame = int_h5MinutesFrame
    if int_h5Minutes.alpha > 0.0 {
         int_h5Minutes.alpha = 0.0
    }
    if int_h5Minutes.isHidden {
         int_h5Minutes.isHidden = false
    }
    if !int_h5Minutes.isUserInteractionEnabled {
         int_h5Minutes.isUserInteractionEnabled = true
    }


    
    var bleedFmtpFrame = bleedFmtp.frame
    bleedFmtpFrame.size = CGSize(width: 75, height: 280)
    bleedFmtp.frame = bleedFmtpFrame
    if bleedFmtp.isHidden {
         bleedFmtp.isHidden = false
    }
    if bleedFmtp.alpha > 0.0 {
         bleedFmtp.alpha = 0.0
    }
    if !bleedFmtp.isUserInteractionEnabled {
         bleedFmtp.isUserInteractionEnabled = true
    }

    return bleedFmtp

}


@discardableResult
 func actionOpenComplete(pageConstraint: Int, infoHolderlabel: Int, currentAlamofire: Bool) -> [String: Any]! {
    var aicelll: Double = 3.0
    var unselectedz: String! = String(cString: [112,114,97,103,109,97,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &unselectedz) { pointer in
          _ = pointer.pointee
   }
    var restoreO: [String: Any]! = [String(cString: [119,95,56,51,0], encoding: .utf8)!:567, String(cString: [113,112,113,115,99,97,108,101,0], encoding: .utf8)!:795, String(cString: [114,101,97,115,115,111,99,105,97,116,101,0], encoding: .utf8)!:713]
    _ = restoreO
   repeat {
      unselectedz.append("\(1)")
      if unselectedz == (String(cString:[114,108,122,120,110,0], encoding: .utf8)!) {
         break
      }
   } while (2 < restoreO.values.count) && (unselectedz == (String(cString:[114,108,122,120,110,0], encoding: .utf8)!))
      unselectedz.append("\((3 & Int(aicelll > 141503670.0 || aicelll < -141503670.0 ? 75.0 : aicelll)))")
      unselectedz.append("\(restoreO.count + unselectedz.count)")
   repeat {
      unselectedz = "\(1)"
      if unselectedz.count == 1287566 {
         break
      }
   } while ((Double(unselectedz.count) * aicelll) > 1.78 && 3 > (unselectedz.count % (Swift.max(4, 3)))) && (unselectedz.count == 1287566)
       var utilsf: Double = 4.0
       _ = utilsf
      repeat {
         utilsf -= (Double(Int(utilsf > 68983239.0 || utilsf < -68983239.0 ? 27.0 : utilsf)))
         if utilsf == 1710420.0 {
            break
         }
      } while ((utilsf / (Swift.max(utilsf, 1))) <= 4.12 && 4.12 <= (utilsf / (Swift.max(4, utilsf)))) && (utilsf == 1710420.0)
         utilsf *= (Double(Int(utilsf > 364130739.0 || utilsf < -364130739.0 ? 77.0 : utilsf)))
         utilsf -= (Double(Int(utilsf > 207110351.0 || utilsf < -207110351.0 ? 39.0 : utilsf)))
      unselectedz = "\(1)"
      aicelll *= Double(restoreO.count)
   return restoreO

}


@discardableResult
 func nextCenter(containsQuick: String!, ollectionClose: [String: Any]!, beginTrue_o: Bool) -> Float {
    var recognizedI: [Any]! = [String(cString: [115,116,97,114,116,114,101,101,0], encoding: .utf8)!, String(cString: [100,101,116,97,105,108,101,100,0], encoding: .utf8)!]
    var tableheadB: String! = String(cString: [99,108,110,112,97,115,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &tableheadB) { pointer in
          _ = pointer.pointee
   }
    var handlerY: Float = 0.0
    _ = handlerY
      recognizedI = [(Int(handlerY > 249537494.0 || handlerY < -249537494.0 ? 42.0 : handlerY) ^ 2)]
       var downloadr: Int = 4
       var sendZ: Double = 5.0
      withUnsafeMutablePointer(to: &sendZ) { pointer in
    
      }
       var topy: [Any]! = [String(cString: [97,99,114,111,110,121,109,0], encoding: .utf8)!, String(cString: [99,111,114,114,101,115,112,111,110,100,101,110,116,115,0], encoding: .utf8)!]
         topy = [topy.count]
      repeat {
         downloadr /= Swift.max(4, 2)
         if downloadr == 4903608 {
            break
         }
      } while (4.59 >= sendZ) && (downloadr == 4903608)
      while (Int(sendZ) == downloadr) {
         downloadr >>= Swift.min(4, labs((Int(sendZ > 177268314.0 || sendZ < -177268314.0 ? 36.0 : sendZ))))
         break
      }
      repeat {
          var scroll0: String! = String(cString: [115,112,114,105,116,101,0], encoding: .utf8)!
          var failedq: String! = String(cString: [117,110,97,117,116,104,111,114,105,122,101,100,0], encoding: .utf8)!
          _ = failedq
          var numberlabelF: String! = String(cString: [112,114,111,116,101,99,116,101,100,0], encoding: .utf8)!
          var maskp: String! = String(cString: [112,114,101,102,105,120,101,100,0], encoding: .utf8)!
          var inputL: String! = String(cString: [109,105,108,105,111,110,115,0], encoding: .utf8)!
         sendZ += Double(inputL.count << (Swift.min(scroll0.count, 5)))
         failedq = "\(numberlabelF.count | topy.count)"
         numberlabelF.append("\(topy.count)")
         maskp.append("\(topy.count)")
         if 2105935.0 == sendZ {
            break
         }
      } while (2105935.0 == sendZ) && ((2 * downloadr) > 5 && (Double(downloadr) - sendZ) > 2.18)
         sendZ *= Double(downloadr)
      if 5 == (downloadr & topy.count) || 3 == (5 & topy.count) {
          var system_: Int = 0
         withUnsafeMutablePointer(to: &system_) { pointer in
                _ = pointer.pointee
         }
          var photo5: Double = 3.0
          var calendar1: String! = String(cString: [102,105,108,108,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &calendar1) { pointer in
    
         }
         topy.append((Int(photo5 > 122549877.0 || photo5 < -122549877.0 ? 92.0 : photo5) / (Swift.max(2, system_))))
         calendar1.append("\(3)")
      }
         downloadr ^= (3 / (Swift.max(5, Int(sendZ > 272051207.0 || sendZ < -272051207.0 ? 1.0 : sendZ))))
          var controlV: [Any]! = [false]
          _ = controlV
         downloadr %= Swift.max((Int(sendZ > 170040049.0 || sendZ < -170040049.0 ? 50.0 : sendZ) + 1), 1)
         controlV = [controlV.count << (Swift.min(labs(3), 5))]
         downloadr |= 3
      handlerY /= Swift.max(Float(3 + recognizedI.count), 5)
       var sandboxO: String! = String(cString: [115,117,98,100,105,118,105,115,105,111,110,0], encoding: .utf8)!
          var itemdataK: Double = 0.0
          var bign: String! = String(cString: [115,99,114,101,101,110,115,0], encoding: .utf8)!
          _ = bign
          var pasteboards: Int = 0
         sandboxO = "\(2)"
         itemdataK *= (Double(Int(itemdataK > 204972113.0 || itemdataK < -204972113.0 ? 96.0 : itemdataK)))
         bign = "\(pasteboards)"
         pasteboards >>= Swift.min(labs(bign.count + 3), 4)
       var elect7: String! = String(cString: [108,105,109,105,116,115,0], encoding: .utf8)!
      if sandboxO.count <= 2 {
         sandboxO = "\(elect7.count % 2)"
      }
      tableheadB.append("\(2 | tableheadB.count)")
   if recognizedI.contains { $0 as? Float == handlerY } {
      handlerY /= Swift.max(Float(3 & tableheadB.count), 3)
   }
      handlerY += Float(tableheadB.count << (Swift.min(5, recognizedI.count)))
   return handlerY

}


@discardableResult
 func writeStoreLoad(jsonSelectbutton: Bool, drawTest: String!) -> UITableView! {
    var purchasedA: String! = String(cString: [97,98,98,114,0], encoding: .utf8)!
    var l_heightt: [String: Any]! = [String(cString: [116,114,97,110,115,113,117,97,110,116,0], encoding: .utf8)!:847, String(cString: [116,111,108,108,0], encoding: .utf8)!:814]
   repeat {
       var controllers5: String! = String(cString: [99,108,101,97,114,0], encoding: .utf8)!
       var slideri: Bool = true
      withUnsafeMutablePointer(to: &slideri) { pointer in
    
      }
       var useri: Float = 1.0
       var linesc: Float = 1.0
          var alabelf: Double = 5.0
         withUnsafeMutablePointer(to: &alabelf) { pointer in
    
         }
         linesc += (Float(Int(linesc > 210576836.0 || linesc < -210576836.0 ? 76.0 : linesc) >> (Swift.min(3, labs((slideri ? 1 : 2))))))
         alabelf /= Swift.max(Double(controllers5.count), 4)
          var screeng: String! = String(cString: [111,110,101,115,0], encoding: .utf8)!
          _ = screeng
          var aymentc: [Any]! = [507, 260, 58]
          var jiaoz: Bool = false
          _ = jiaoz
         slideri = (((slideri ? 34 : controllers5.count) - controllers5.count) <= 34)
         screeng.append("\(screeng.count)")
         aymentc.append(3)
         jiaoz = controllers5.count == 84
      repeat {
          var processing8: Int = 5
          var settingJ: Bool = false
          var openw: String! = String(cString: [114,101,113,117,101,115,116,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &openw) { pointer in
                _ = pointer.pointee
         }
         useri -= (Float(processing8 + (settingJ ? 5 : 3)))
         openw.append("\((openw == (String(cString:[86,0], encoding: .utf8)!) ? openw.count : (settingJ ? 3 : 1)))")
         if 4835795.0 == useri {
            break
         }
      } while (!slideri) && (4835795.0 == useri)
      repeat {
          var fixedO: String! = String(cString: [114,101,115,117,108,116,105,110,103,0], encoding: .utf8)!
          var pasteboardi: String! = String(cString: [98,97,116,99,104,105,110,103,0], encoding: .utf8)!
          var sectiono: [String: Any]! = [String(cString: [118,108,111,103,0], encoding: .utf8)!:String(cString: [114,101,103,105,115,116,101,114,101,100,0], encoding: .utf8)!, String(cString: [109,105,110,114,0], encoding: .utf8)!:String(cString: [117,116,102,108,101,110,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &sectiono) { pointer in
                _ = pointer.pointee
         }
          var restoreN: String! = String(cString: [100,105,115,97,98,108,101,0], encoding: .utf8)!
          _ = restoreN
          var statuslabelb: String! = String(cString: [109,117,108,104,105,0], encoding: .utf8)!
         linesc += Float(pasteboardi.count)
         fixedO.append("\(restoreN.count % (Swift.max(2, statuslabelb.count)))")
         sectiono[fixedO] = fixedO.count / 2
         restoreN.append("\(controllers5.count)")
         statuslabelb = "\(fixedO.count / 1)"
         if linesc == 4809303.0 {
            break
         }
      } while (linesc == 4809303.0) && (3.49 <= (Float(controllers5.count) + linesc) || 5 <= (3 | controllers5.count))
      if controllers5.count == 2 || slideri {
         slideri = !slideri || 14.58 >= linesc
      }
      l_heightt["\(slideri)"] = ((slideri ? 1 : 1) | 1)
      if l_heightt.count == 3282507 {
         break
      }
   } while (l_heightt.count == 3282507) && ((l_heightt.keys.count + purchasedA.count) < 3)
      purchasedA.append("\(1)")
      purchasedA = "\(purchasedA.count / 1)"
   for _ in 0 ..< 2 {
      l_heightt = ["\(l_heightt.keys.count)": l_heightt.values.count]
   }
     var attributedOrientation: UILabel! = UILabel(frame:CGRect(x: 84, y: 355, width: 0, height: 0))
     let deepCount: UIView! = UIView(frame:CGRect(x: 243, y: 98, width: 0, height: 0))
     let questWorkbutton: Double = 7955.0
    var chromiumSpenderIntraxhuf = UITableView(frame:CGRect(x: 152, y: 75, width: 0, height: 0))
    chromiumSpenderIntraxhuf.dataSource = nil
    chromiumSpenderIntraxhuf.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    chromiumSpenderIntraxhuf.delegate = nil
    chromiumSpenderIntraxhuf.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    chromiumSpenderIntraxhuf.alpha = 0.1
    chromiumSpenderIntraxhuf.frame = CGRect(x: 125, y: 155, width: 0, height: 0)
    attributedOrientation.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    attributedOrientation.alpha = 0.7
    attributedOrientation.frame = CGRect(x: 16, y: 58, width: 0, height: 0)
    attributedOrientation.textAlignment = .right
    attributedOrientation.font = UIFont.systemFont(ofSize:18)
    attributedOrientation.text = ""
    attributedOrientation.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    var attributedOrientationFrame = attributedOrientation.frame
    attributedOrientationFrame.size = CGSize(width: 147, height: 199)
    attributedOrientation.frame = attributedOrientationFrame
    if attributedOrientation.alpha > 0.0 {
         attributedOrientation.alpha = 0.0
    }
    if attributedOrientation.isHidden {
         attributedOrientation.isHidden = false
    }
    if !attributedOrientation.isUserInteractionEnabled {
         attributedOrientation.isUserInteractionEnabled = true
    }

    deepCount.frame = CGRect(x: 271, y: 219, width: 0, height: 0)
    deepCount.alpha = 0.0;
    deepCount.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    var deepCountFrame = deepCount.frame
    deepCountFrame.size = CGSize(width: 189, height: 172)
    deepCount.frame = deepCountFrame
    if deepCount.isHidden {
         deepCount.isHidden = false
    }
    if deepCount.alpha > 0.0 {
         deepCount.alpha = 0.0
    }
    if !deepCount.isUserInteractionEnabled {
         deepCount.isUserInteractionEnabled = true
    }

         var tmp_f_52 = Int(questWorkbutton)
     switch tmp_f_52 {
          case 47:
          tmp_f_52 *= 33
     break
          case 51:
          var l_55 = 1
     let z_97 = 0
     if tmp_f_52 > z_97 {
         tmp_f_52 = z_97
     }
     while l_55 < tmp_f_52 {
         l_55 += 1
          tmp_f_52 -= l_55
     var n_67 = l_55
          if n_67 >= 625 {
          n_67 /= 42
          }
         break
     }
     break
          case 55:
          tmp_f_52 += 51
          var o_19: Int = 0
     let g_29 = 2
     if tmp_f_52 > g_29 {
         tmp_f_52 = g_29

     }
     if tmp_f_52 <= 0 {
         tmp_f_52 = 2

     }
     for q_54 in 0 ..< tmp_f_52 {
         o_19 += q_54
     var k_18 = o_19
              break

     }
     break
          case 60:
          tmp_f_52 += 18
          tmp_f_52 /= 59
     break
     default:()

     }

    
    var chromiumSpenderIntraxhufFrame = chromiumSpenderIntraxhuf.frame
    chromiumSpenderIntraxhufFrame.size = CGSize(width: 191, height: 164)
    chromiumSpenderIntraxhuf.frame = chromiumSpenderIntraxhufFrame
    if chromiumSpenderIntraxhuf.isHidden {
         chromiumSpenderIntraxhuf.isHidden = false
    }
    if chromiumSpenderIntraxhuf.alpha > 0.0 {
         chromiumSpenderIntraxhuf.alpha = 0.0
    }
    if !chromiumSpenderIntraxhuf.isUserInteractionEnabled {
         chromiumSpenderIntraxhuf.isUserInteractionEnabled = true
    }

    return chromiumSpenderIntraxhuf

}


@discardableResult
 func size_uMaterialBefore() -> UITableView! {
    var ratioF: String! = String(cString: [99,108,97,105,109,101,100,0], encoding: .utf8)!
    var parameter0: String! = String(cString: [110,101,119,108,105,110,101,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &parameter0) { pointer in
    
   }
   while (parameter0 == String(cString:[53,0], encoding: .utf8)! || 2 < ratioF.count) {
      parameter0 = "\(((String(cString:[113,0], encoding: .utf8)!) == parameter0 ? ratioF.count : parameter0.count))"
      break
   }
      ratioF = "\(parameter0.count ^ 3)"
   for _ in 0 ..< 2 {
       var storeB: String! = String(cString: [102,114,97,109,101,112,111,111,108,0], encoding: .utf8)!
       var template_rcL: String! = String(cString: [112,114,105,109,97,114,121,0], encoding: .utf8)!
       var btnh: Float = 5.0
      withUnsafeMutablePointer(to: &btnh) { pointer in
    
      }
      while (Float(storeB.count) >= btnh) {
         storeB.append("\(template_rcL.count * 3)")
         break
      }
      while (!template_rcL.contains("\(btnh)")) {
          var thresholdD: String! = String(cString: [102,105,116,116,105,110,103,0], encoding: .utf8)!
          var o_layerY: Int = 2
          _ = o_layerY
          var avatarQ: String! = String(cString: [100,101,99,111,100,97,98,108,101,0], encoding: .utf8)!
          var rawingT: Double = 5.0
         template_rcL = "\(thresholdD.count)"
         o_layerY %= Swift.max(2, template_rcL.count + 1)
         avatarQ = "\(2 >> (Swift.min(3, template_rcL.count)))"
         rawingT *= Double(template_rcL.count % (Swift.max(avatarQ.count, 2)))
         break
      }
      if (template_rcL.count - 4) < 2 {
          var minimumI: Bool = true
          var strokeI: String! = String(cString: [104,115,118,97,0], encoding: .utf8)!
         template_rcL.append("\(storeB.count << (Swift.min(3, strokeI.count)))")
         minimumI = template_rcL.count >= 65 || 65 >= strokeI.count
      }
      while (!template_rcL.contains(storeB)) {
          var listC: Double = 3.0
          var tablee4: String! = String(cString: [109,98,114,116,104,114,101,97,100,0], encoding: .utf8)!
          _ = tablee4
          var boardy8: Double = 3.0
         template_rcL.append("\(3)")
         listC *= Double(storeB.count & 2)
         tablee4.append("\(tablee4.count)")
         boardy8 /= Swift.max((Double(Int(btnh > 25228476.0 || btnh < -25228476.0 ? 20.0 : btnh) ^ Int(listC > 237353750.0 || listC < -237353750.0 ? 27.0 : listC))), 2)
         break
      }
          var avatar5: Int = 4
         template_rcL.append("\(template_rcL.count)")
         avatar5 += 1
      repeat {
         storeB = "\(2 & storeB.count)"
         if (String(cString:[48,111,122,50,53,0], encoding: .utf8)!) == storeB {
            break
         }
      } while (template_rcL == storeB) && ((String(cString:[48,111,122,50,53,0], encoding: .utf8)!) == storeB)
       var testz: String! = String(cString: [109,105,110,105,109,97,108,0], encoding: .utf8)!
       _ = testz
       var v_centerj: String! = String(cString: [100,101,99,114,101,109,101,110,116,0], encoding: .utf8)!
       _ = v_centerj
      repeat {
         testz = "\(storeB.count)"
         if 4098090 == testz.count {
            break
         }
      } while (testz == String(cString:[107,0], encoding: .utf8)! || template_rcL.count < 1) && (4098090 == testz.count)
      repeat {
         storeB.append("\((Int(btnh > 354059780.0 || btnh < -354059780.0 ? 38.0 : btnh) / 2))")
         if storeB == (String(cString:[103,49,114,119,0], encoding: .utf8)!) {
            break
         }
      } while (storeB == (String(cString:[103,49,114,119,0], encoding: .utf8)!)) && (storeB.contains("\(btnh)"))
         v_centerj = "\(2 * template_rcL.count)"
      parameter0.append("\(2)")
   }
   while (ratioF.count < 3) {
      ratioF.append("\(2 & ratioF.count)")
      break
   }
     var celllMorebutton: String! = String(cString: [108,117,109,97,107,101,121,0], encoding: .utf8)!
     let contenPerform: String! = String(cString: [105,116,115,101,108,102,0], encoding: .utf8)!
     var animaGenerator: Bool = true
    var strikethroughMbstring:UITableView! = UITableView()
    strikethroughMbstring.delegate = nil
    strikethroughMbstring.dataSource = nil
    strikethroughMbstring.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    strikethroughMbstring.alpha = 0.3;
    strikethroughMbstring.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    strikethroughMbstring.frame = CGRect(x: 186, y: 151, width: 0, height: 0)

    
    var strikethroughMbstringFrame = strikethroughMbstring.frame
    strikethroughMbstringFrame.size = CGSize(width: 208, height: 108)
    strikethroughMbstring.frame = strikethroughMbstringFrame
    if strikethroughMbstring.alpha > 0.0 {
         strikethroughMbstring.alpha = 0.0
    }
    if strikethroughMbstring.isHidden {
         strikethroughMbstring.isHidden = false
    }
    if !strikethroughMbstring.isUserInteractionEnabled {
         strikethroughMbstring.isUserInteractionEnabled = true
    }

    return strikethroughMbstring

}




}